import 'package:flutter/widgets.dart';

class ScreenEffectsWidget extends StatelessWidget {
  const ScreenEffectsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
